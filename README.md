# Многопользовательский сетевой чат на Java

Этот проект представляет собой простой **TCP-чат**, в котором несколько клиентов могут подключаться к серверу, обмениваться сообщениями в реальном времени и видеть полную историю переписки.

## 🔧 Технологии

- Java 21
- Maven
- `java.net.Socket` / `ServerSocket`
- `java.util.logging`
- UTF-8 кодировка (поддержка кириллицы)

## 📁 Структура проекта

├── src
│ └── main
│ └── java
│ └── org/example
│ ├── Server.java # Сервер чата
│ ├── ClientHandler.java # Обработчик клиента (многопоточность)
│ └── ChatClient.java # Консольный клиент
├── settings.txt # Порт сервера (по умолчанию 8089)
├── file.log # Лог-файл (создаётся автоматически)
├── pom.xml
└── README.md

## ▶️ Как запустить

### 1. Сборка проекта

```bash
mvn compile
java -cp target/classes org.example.Server /Запуск сервера

в отдельном терминале / если нужно - несколько
java -cp target/classes org.example.ChatClient / Запуск клиента

Введите своё имя.
Общайтесь! Все сообщения (включая свои) отображаются мгновенно.
Чтобы выйти, введите: /exit

🧪 Тестирование в IntelliJ IDEA
Запустите Server.main() через зелёную стрелку.
Включите опцию «Allow parallel run» в Run Configuration для ChatClient.
Запускайте ChatClient.main() несколько раз — каждый запуск создаёт нового клиента.

